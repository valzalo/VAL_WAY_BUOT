# Category-D
The purpose of this project is for practical Examination for Set-D


If Python is not installed
Run this in the Codespaces terminal:
sudo apt update sudo apt install python3 -y

Check if Python is installed:
python3 --version

Install SQLITE3
sudo apt update sudo apt install sqlite3 -y

Check SQLite3
sqlite3 --version

"""
ELT Practical Exam
Topic: Structured and Unstructured Data using SQLite3

Pipeline:
Structured CSV + Unstructured TXT → Extract → Load → Transform → Select

Student Task:
Complete only the SQLite3 parts:

1. CREATE TABLE
2. INSERT INTO
3. SELECT FROM

Note:
The transformation process is already provided.
"""